server:
  port: 8080

management:
  endpoints:
    web:
      base-path: /cos/hk/v1/master-records
info:
  app:
    name: @project.name@
    description: @project.description@
    version: @project.version@
    encoding: @project.build.sourceEncoding@
spring:
  jpa:
    properties:
      hibernate:
        default_schema: coseg_dev
        show_sql: true
        temp:
          use_jdbc_metadata_defaults: false
    database-platform: org.hibernate.dialect.PostgreSQL9Dialect
  datasource:
    url: jdbc:postgresql://cosegdb-development.c2xwsmr3mbpf.eu-west-1.rds.amazonaws.com:5401/cosegdb
    username: db_admin
    password: admin123    
    hikari:
      connection-timeout: 30000
      maximum-pool-size: 1